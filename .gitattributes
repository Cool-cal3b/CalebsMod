# Shell scripts must reach macOS with LF endings. A .sh checked out with
# CRLF fails on the friend-facing install path with "$'\r': command not
# found", which is a confusing way to find out about line endings.
*.sh text eol=lf
